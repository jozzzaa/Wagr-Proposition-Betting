<%= form_tag charges_path do %>
  <article>
    <% if flash[:error].present? %>
      <div id="error_explanation">
        <p><%= flash[:error] %></p>
      </div>
    <% end %>
    <label class="amount">
      <span style="color: white">Credit $50 your Wagr Account</span>
    </label>

  </article>
  <input type="hidden" name="custom_amount" value="5000">
  <script src="https://checkout.stripe.com/checkout.js" class="stripe-button"
          data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
          data-description="Credit Wagr Account"
          data-amount="5000"
          data-locale="auto"></script>
  <% end %>

  <%= form_tag charges_path do %>
    <article>
      <% if flash[:error].present? %>
        <div id="error_explanation">
          <p><%= flash[:error] %></p>
        </div>
      <% end %>
      <label class="amount">
        <span style="color: white">Credit $100 your Wagr Account</span>
      </label>

    </article>
    <input type="hidden" name="custom_amount" value="10000">
    <script src="https://checkout.stripe.com/checkout.js" class="stripe-button"
            data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
            data-description="Credit Wagr Account"
            data-amount="10000"
            data-locale="auto"></script>
    <% end %>

    <%= form_tag charges_path do %>
      <article>
        <% if flash[:error].present? %>
          <div id="error_explanation">
            <p><%= flash[:error] %></p>
          </div>
        <% end %>
        <label class="amount">
          <span style="color: white">Credit $150 your Wagr Account</span>
        </label>

      </article>
      <input type="hidden" name="custom_amount" value="15000">
      <script src="https://checkout.stripe.com/checkout.js" class="stripe-button"
              data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
              data-description="Credit Wagr Account"
              data-amount="15000"
              data-locale="auto"></script>
      <% end %>
